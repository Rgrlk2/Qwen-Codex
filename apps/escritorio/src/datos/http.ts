/**
 * Implementación HTTP de `CapaDatos`.
 *
 * ⛔ DUEÑO: SESIÓN 1.
 *
 * Única responsabilidad: traducir HTTP ⇄ `Resultado<T>` según docs/API_CONTRACTS.md §1,
 * incluida la tabla de códigos HTTP → CodigoError. Ningún `mensajeAmable` puede
 * contener un código HTTP, un nombre de tabla ni una traza.
 */

export {};

{
  "name": "escritorio-vendedores-labia",
  "version": "0.1.0",
  "private": true,
  "description": "Escritorio Vendedores Lab.IA — especificacion, contratos compartidos y esqueleto del monorepo",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -b packages/compartido",
    "verificar:copy": "cd content/copy && sha256sum -c copy.sha256",
    "verificar:portafolio": "node scripts/verificar-portafolio.mjs",
    "verificar": "npm run verificar:copy && npm run verificar:portafolio && npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
